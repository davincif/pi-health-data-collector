services:
  pi-health-watcher:
    build: .
    image: ldavincif/pi-health-watcher
    container_name: pi-health-watcher
    restart: unless-stopped
    uts: "host"
    security_opt:
      - no-new-privileges:true
    environment:
      - PORT=${PORT:-7325}
      - ADDR=${ADDR:-192.168.1.153}
